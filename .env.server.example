# SAP MCP Server Configuration
#
# This is an example configuration file for the SAP MCP server.
# Copy this file to .env.server and configure with your actual SAP credentials.
#
# Setup Instructions:
#   1. Copy this file: cp .env.server.example .env.server
#   2. Edit .env.server with your SAP Gateway credentials
#   3. Set proper permissions: chmod 600 .env.server
#   4. Never commit .env.server to version control

# ============================================================================
# SAP Gateway Connection Settings (REQUIRED)
# ============================================================================

# SAP Gateway hostname or IP address
# Example: sap-gateway.company.com or 192.168.1.100
SAP_HOST=your-sap-gateway-host.com

# SAP Gateway port number
# Common values: 443 (HTTPS), 8443 (custom HTTPS), 44300 (development)
SAP_PORT=443

# SAP client number (3-digit code)
# Common values: 100 (development), 200 (quality), 300 (production)
# Check with your SAP administrator for the correct client number
SAP_CLIENT=100

# SAP username for authentication
# Use a dedicated service account for MCP server (recommended)
SAP_USERNAME=your-sap-username

# SAP password for authentication
# SECURITY WARNING: Store securely and never commit to version control
SAP_PASSWORD=your-sap-password

# ============================================================================
# SAP Connection Settings (OPTIONAL)
# ============================================================================

# SSL certificate verification
# Values: true (recommended for production), false (development only)
# Set to false only for self-signed certificates in development
SAP_VERIFY_SSL=true

# Request timeout in seconds
# Increase for slow networks or complex queries
# Default: 30 seconds
SAP_TIMEOUT=30

# Number of retry attempts on connection failure
# Recommended: 3 for production, 1 for development
SAP_RETRY_ATTEMPTS=3

# ============================================================================
# MCP Server Configuration (OPTIONAL)
# ============================================================================
# Note: These settings are used by HTTP transport (not yet implemented)
# For stdio transport (current), these settings are not required

# MCP server bind address
# Values: 0.0.0.0 (all interfaces), 127.0.0.1 (localhost only)
# MCP_HOST=0.0.0.0

# MCP server port
# MCP_PORT=8000

# Logging level
# Values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# MCP_LOG_LEVEL=INFO

# Maximum number of worker processes
# MCP_MAX_WORKERS=1

# Enable debug mode
# Values: true, false
# MCP_DEBUG=false

# Enable auto-reload on code changes
# Values: true, false
# MCP_RELOAD=false

# ============================================================================
# Security Configuration (OPTIONAL)
# ============================================================================

# Session timeout in seconds (1 hour = 3600)
# SECURITY_SESSION_TIMEOUT=3600

# Maximum concurrent sessions allowed
# SECURITY_MAX_CONCURRENT_SESSIONS=100

# Rate limit: maximum requests per minute
# SECURITY_RATE_LIMIT_PER_MINUTE=60

# ============================================================================
# Additional Notes
# ============================================================================
#
# 1. Required Fields:
#    - SAP_HOST, SAP_PORT, SAP_CLIENT, SAP_USERNAME, SAP_PASSWORD
#
# 2. Security Best Practices:
#    - Enable SSL verification in production (SAP_VERIFY_SSL=true)
#    - Use strong, unique passwords
#    - Restrict file permissions: chmod 600 .env.server
#    - Never commit .env.server to version control
#    - Use dedicated service accounts for MCP server
#
# 3. File Location:
#    - Must be in the project root directory
#    - Path: /path/to/sap-mcp/.env.server
#
# 4. Testing Configuration:
#    source .venv/bin/activate
#    python -c "from sap_mcp_server.config.settings import get_config; print(get_config(require_sap=True).sap)"
#
# 5. Troubleshooting:
#    - If authentication fails, verify credentials with SAP administrator
#    - Check network connectivity to SAP Gateway
#    - Ensure SAP client number is correct
#    - For self-signed certificates, set SAP_VERIFY_SSL=false (development only)
#
