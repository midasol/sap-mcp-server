# SAP MCP Client Configuration
# Copy this file to .env.client for client-side configuration

# ==========================================
# Client Configuration (OPTIONAL)
# ==========================================
# Currently, the MCP client connects to the server via stdio,
# so it doesn't need separate configuration.
# The server handles all SAP connections using .env.server

# Future client-side configuration can be added here:
# MCP_SERVER_URL=http://localhost:8000
# CLIENT_TIMEOUT=60
# CLIENT_LOG_LEVEL=INFO
